<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="shortcut icon" href="images/favicon.ico" type="image/x-icon" />
    <link rel="stylesheet" href="/css/common.css" />
    <link rel="stylesheet" href="/css/usage.css" />
    <title>우리들의 사용량 보기</title>

    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
  <script
  src="https://kit.fontawesome.com/8ddf9e168a.js"
  crossorigin="anonymous"
></script>

  </head>
  <body>
    <header id="header">
      <div class="desktopHeader">
        <div class="logo">
          <a href="/">SaveTheWorld</a>
        </div>
        <nav>
          <li><a href="/about">소개</a></li>
          <li><a href="/usage?id=1">우리들의 사용량</a></li>
          <div class="loginId">
            <% if(nickname){%>
            <span class="loging"><%=nickname%>님 환영합니다.</span>
            <span class="logout"
              ><a href="/logout"
                ><i class="fa-solid fa-right-from-bracket"></i></a
            ></span>
            <%} else {%>
            <li><a href="/login">로그인</a></li>
            <%}%>
          </div>
        </nav>
      </div>
    </header>

    <div class="line_wrap">우리들의 사용량 보기</div>

    <div class="write_wrap">
      <div class="write" id="getWrite">글쓰기</div>
    </div>

    <div class="usage_wrapper">
      <div class="usage_wrap">
        <table class="table">
          <thead>
            <tr>
              <th width="10%">번호</th>
              <th width="10%">지역</th>
              <th width="10%">분류</th>
              <th with="35%">제목</th>
              <th width="10%">아이디</th>
              <th width="10%">날짜</th>
            </tr>
          </thead>
          <tbody>
            <% page.forEach(el => {%>
              <td><%=el.id%></td>
              <td><%=el.area%></></td>
              <td><%=el.sort%></td>
              <td>
                <a href="/detail/<%=el.id%>"><%=el.title%></a>
              </td>
              <td><%=el.nickname%></td>
              <td><%=el.createdAt%></td>
            </tr>          
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>
    <!-- page_wrap -->

    <div class="page_wrap">
      <div class="paging">
        <div class="ctrlBack">
          <a href="/usage?id=1" id="leftStart" class="leftStart">
            <i class="arrow fa-solid fa-angles-left"></i>
          </a>
          <% if( Number(currentPage) <= 1) { %>
            <a href="/usage?id=<%= 1 %>" id="left" class="right">
              <i class="arrow fa-solid fa-angle-left"></i>
            </a>
          <% } else { %>
            <a href="/usage?id=<%=  Number(currentPage) - 1 %>" id="left" class="right">
              <i class="arrow fa-solid fa-angle-left"></i>
            </a>
          <% } %>
         

          <div class="pageNum" id="pageNum">
          <% if(pageCount >= 5 ) { %>
            <% for ( let i = 0 ; i < 5 ; i++) { %>
              <a class="pageBtn <%= Number(currentPage) === i + 1 ? 'active':'' %>" href="/usage?id=<%= i+1 %>"><%= i + 1 %></a>
            <% } %>
          <% } else { %>
            <% for ( let i = 0 ; i < pageCount ; i++) { %>
              <a class="pageBtn <%= Number(currentPage) === i + 1 ? 'active':'' %>" href="/usage?id=<%= i+1 %>"><%= i + 1 %></a>
            <% } %>
          <% } %>
          </div>
          <% if( Number(currentPage) >= pageCount) { %>
            <a href="/usage?id=<%= pageCount %>" id="right" class="right">
              <i class="arrow fa-solid fa-angle-right"></i>
            </a>
          <% } else { %>
            <a href="/usage?id=<%= Number(currentPage) + 1 %>" id="right" class="right">
              <i class="arrow fa-solid fa-angle-right"></i>
            </a>
          <% } %>

          <a href="/usage?id=<%= pageCount %>" id="rightEnd" class="rightEnd">
            <i class="arrow fa-solid fa-angles-right"></i>
          </a>
        </div>
      </div>
    </div>

    <footer id="footer">
      <div class="footerText">문의 : sammy342@naver.com</div>
    </footer>


    <script src="/js/usage.js"></script>
    <script src="/js/write.js"></script>
  </body>
</html>